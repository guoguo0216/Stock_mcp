{
  "name": "a-share-stock-quote-mcp",
  "version": "1.0.0",
  "description": "A股实时行情 MCP Server，部署在 Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
